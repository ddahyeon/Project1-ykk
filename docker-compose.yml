services:
  mysql:
    image: mysql:8.3
    container_name: ykk-mysql
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_DATABASE: ykkdb
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    networks:
      - ykk-net

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: dahyeoni/ykk-backend:v1
    container_name: ykk-backend
    ports:
      - "8080:8080"
    environment:
      DB_URL: jdbc:mysql://mysql:3306/ykkdb
      DB_HOST: mysql
      DB_PORT: 3306
      DB_NAME: ykkdb
      DB_USERNAME: root
      DB_PASSWORD: 1234
    depends_on:
      - mysql
    networks:
      - ykk-net

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: dahyeoni/ykk-frontend:v1
    container_name: ykk-frontend
    ports:
      - "80:80"
    depends_on:
    - backend
    networks:
      - ykk-net

volumes:
  mysql-data:

networks:
  ykk-net: