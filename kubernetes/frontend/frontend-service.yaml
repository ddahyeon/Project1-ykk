# frontend-service
apiVersion: v1
kind: Service
metadata:
  name: ykk-frontend-service
  namespace: ykk
spec: 
  selector:
    app: ykk-frontend      # Frontend Pod와 연결
  ports:
    - port: 80             # Service 포트
      targetPort: 80       # Frontend Pod 포트
  type: ClusterIP