# 07-backend-service.yaml

apiVersion: v1
kind: Service
metadata:
  name: ykk-backend-service
  namespace: ykk
spec:
  selector:
    app: ykk-backend
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP